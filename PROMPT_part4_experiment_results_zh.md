# 結構化 Prompt：Part 4 — 實驗與結果（以下整段可複製給 LLM）

---

## 角色（Role）

你協助準備大學深度學習課程簡報的 **Part 4：實驗與結果**。負責人講的是 **評估設定**、**呈現的指標** 與 **結論**，**不要**重講完整模型架構（前面部分已講過）。

---

## 背景（專案 Context）

- **主題：** 適應多粒度神經交易系統 — 以 **OHLCV + 技術特徵** 餵 **LSTM**，做下一根 K 棒 **漲跌方向**（二元分類），在 **同一套回測設定** 下與基準比較（**Beat the Classics**）。
- **基準（非深度學習）：** Buy-and-Hold、MACD、RSI、布林帶（Bollinger）。
- **管線（概念）：** 下載資料 → 特徵工程 → 依時間切分 **train / val / test** → 訓練 LSTM → 在 **test** 報告 **分類指標** → 在 **同一 test 視窗** 做 **回測** 並扣 **手續費** → 與基準比 **報酬與風險**。
- **前面已涵蓋：** 資料來源、特徵、切分比例、Scaler 僅在 train fit、LSTM 架構、損失、優化器。**請勿再細講這些。**

---

## 任務（Task）

只產出 **Part 4** 適用的內容：

1. **整體故事（可選開場，約 30 秒）：** 輸入資料 → 訓練 → 分類指標 → 回測 → 賺賠／風險。
2. **Slide 1 — Setup（評估／回測設定）：** 比較為何公平（測試段、手續費、五條策略並列）。
3. **Slide 2 — Numbers：** **TEST** 上的分類（Accuracy、Precision、Recall、**F1**）+ 財務（總報酬、Sharpe、最大回撤），**LSTM 對四基準**；必要時可提產物檔名（`*_bt_summary.json`、`comparison_summary.csv`）。
4. **Slide 3 — Verdict：** 一張圖（權益曲線或長條圖）+ **一句話結論**（全勝／部分／未勝）；若 LSTM 未贏基準，**誠實說明**。

---

## 範圍（Scope）

| 要講 | 不要講（交給其他 Part） |
|------|-------------------------|
| 回測／評估設定、公平性 | 特徵逐欄推導 |
| 測試集分類指標 | LSTM 逐層數學細節 |
| 財務指標與解讀 | Part 5 限制（除非一句帶過） |
| 數字從哪來（檔案／切分） | 實盤下單／投資建議 |

---

## 限制（Constraints）

- **語言：** 依使用者要求（中文／英文／雙語）；若簡報混用，**標題可預設英文**。
- **正確性：** 須與專案行為一致：**時間序切分**；Scaler **只在 train** fit；回測在 **test**；**同一 fee**；LSTM 訊號由 **機率 vs threshold** 決定多／空（依實作）。
- **語氣：** 學術、簡潔；**不**宣稱穩賺；若適用，結果需 **附條件**（標的、頻率、測試窗）。

---

## 輸出格式（必填）

請依序輸出：

1. **`## Overall（最多 5 點）`** — 每點一行：輸入 → 訓練 → 指標 → 回測 → 損益／風險。
2. **`## Slide 1 — Setup`** — 標題 + 副標 + 3–5 個 bullet。
3. **`## Slide 2 — Numbers`** — 標題 + 副標 + 兩小塊（分類｜財務）+ 總共 3–5 個 bullet。
4. **`## Slide 3 — Verdict`** — 標題 + 副標 + 圖說明要點 + **一句話結論範本**（贏／部分贏／沒贏）。
5. **`## Speaker notes（選填）`** — 每頁 2–3 句口語講稿。

若使用者貼上 **數字表或 JSON 片段**，bullet 與結論 **只允許** 依該內容撰寫，勿捏造數字。

---

## 使用者填寫區（貼上 prompt 前請補齊）

```text
標的 TICKER(S)：
頻率 INTERVAL（例：1d / 1h）：
測試窗描述 TEST WINDOW（例：時間軸最後 15%）：
手續費 FEE（例：0.001）：
附件 ATTACH（選填）：分類表 / bt_summary 節錄 / 圖表說明
```

---

## Prompt 結尾說明

將 **角色** 起至 **使用者填寫區** 整段複製到 LLM，填好變數區後再送出。
